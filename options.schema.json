{
	"title": "options",
	"type": "object",
	"properties": {
        "domElement": {
            "title": "domElement",
            "type": "string"
        },
		"endpoint": {
            "title": "endpoint",
            "format": "uri"
		},
        "typeof": {
            "title": "typeof",
            "type": "object",
            "properties": {
                "callbacks": {
                    "properties": {
                        "onClick": { "pattern": "function", "title": "onClick", "type": "string" }
                    },
                    "required": ["onClick"]
                },
                "events": {
                    "properties": {
                        "onClick": { "pattern": "function", "title": "onClick", "type": "string" }
                    }
                }
            }
        }
	},
	"required": ["domElement", "endpoint", "typeof"],
    "anyOf": [
        {
            "events": {
                "type": "object",
                "properties": {
                    "started": { "type": "string" },
                    "onClick": { "type": "string" },
                    "mouseover": { "type": "string" },
                    "mouseout": { "type": "string" }
                },
                "required": ["onClick"]
            }
        },
        {
            "callbacks": {
                "title": "callbacks",
                "type": "object",
                "properties": {
                    "onClick": {}
                }
            }
        }
    ]
}